# 📚 QR 코드 리더기 & 생성기 제작 튜토리얼

> **수업 시간**: 2~3시간  
> **난이도**: 초급~중급  
> **선수 지식**: Python 기초, 웹 개발 기초 개념

---

## 📋 목차

1. [학습 목표](#-학습-목표)
2. [사전 준비](#-사전-준비)
3. [이론 학습](#-이론-학습)
4. [실습 1: 개발 환경 설정](#-실습-1-개발-환경-설정)
5. [실습 2: QR 코드 리더기 구현](#-실습-2-qr-코드-리더기-구현)
6. [실습 3: QR 코드 생성기 구현](#-실습-3-qr-코드-생성기-구현)
7. [심화 학습](#-심화-학습)
8. [과제](#-과제)
9. [참고 자료](#-참고-자료)

---

## 🎯 학습 목표

이 수업을 통해 다음을 학습합니다:

### 기술적 목표
- ✅ Streamlit을 활용한 웹 애플리케이션 개발
- ✅ 이미지 처리 라이브러리 (Pillow, OpenCV) 사용법
- ✅ QR 코드 인코딩/디코딩 원리 이해
- ✅ 세션 상태 관리 및 사용자 인터랙션 구현
- ✅ Custom CSS를 활용한 UI/UX 디자인

### 실무 역량
- ✅ 실용적인 도구 제작 능력
- ✅ 예외 처리 및 사용자 친화적 에러 메시지 작성
- ✅ 코드 모듈화 및 함수 설계
- ✅ 문서화 및 주석 작성

---

## 🛠️ 사전 준비

### 1. 필요한 소프트웨어

| 소프트웨어 | 버전 | 용도 |
|-----------|------|------|
| Python | 3.8 이상 | 프로그래밍 언어 |
| pip | 최신 버전 | 패키지 관리자 |
| 코드 에디터 | VS Code 권장 | 코드 작성 |
| 웹 브라우저 | Chrome/Edge | 애플리케이션 테스트 |

### 2. 설치 확인

터미널에서 다음 명령어를 실행하여 설치를 확인하세요:

```bash
python --version
# 출력 예: Python 3.12.0

pip --version
# 출력 예: pip 23.x.x
```

### 3. 프로젝트 폴더 생성

```bash
mkdir qr_code_app
cd qr_code_app
```

---

## 📖 이론 학습

### 1. QR 코드란?

**QR 코드 (Quick Response Code)**는 2차원 바코드로, 다양한 데이터를 시각적 패턴으로 인코딩합니다.

#### QR 코드의 구조

```
┌─────────────────────────┐
│ ▪️ ⬜ ⬜ ▪️ ⬜ ▪️ ⬜ ⬜ ▪️ │  ← 위치 패턴 (Position Pattern)
│ ⬜ ▪️ ▪️ ⬜ ▪️ ⬜ ▪️ ▪️ ⬜ │
│ ⬜ ⬜ ▪️ ▪️ ▪️ ▪️ ▪️ ⬜ ⬜ │  ← 정렬 패턴 (Alignment Pattern)
│ ▪️ ⬜ ▪️ ⬜ ▪️ ⬜ ▪️ ⬜ ▪️ │
│ ⬜ ▪️ ⬜ ▪️ ⬜ ▪️ ⬜ ▪️ ⬜ │  ← 데이터 영역 (Data Area)
│ ▪️ ▪️ ▪️ ▪️ ▪️ ▪️ ▪️ ▪️ ▪️ │
└─────────────────────────┘
```

#### QR 코드의 오류 복구 수준

| 수준 | 복구율 | 설명 |
|------|--------|------|
| **L** | ~7% | 가장 작은 QR 코드, 손상에 약함 |
| **M** | ~15% | 균형잡힌 선택 (권장) |
| **Q** | ~25% | 더 안전하지만 크기 증가 |
| **H** | ~30% | 로고 삽입 가능, 가장 큼 |

### 2. Streamlit이란?

**Streamlit**은 Python으로 웹 애플리케이션을 빠르게 만들 수 있는 오픈소스 프레임워크입니다.

#### Streamlit의 장점
- 🚀 **빠른 개발**: HTML/CSS/JavaScript 불필요
- 🔄 **실시간 업데이트**: 코드 수정 시 자동 새로고침
- 🎨 **내장 컴포넌트**: 버튼, 슬라이더, 파일 업로더 등
- 📊 **데이터 시각화**: 차트, 테이블 등 기본 제공

#### 기본 구조

```python
import streamlit as st

# 타이틀 표시
st.title('내 첫 Streamlit 앱')

# 텍스트 입력
name = st.text_input('이름을 입력하세요')

# 버튼 클릭 시 동작
if st.button('인사하기'):
    st.write(f'안녕하세요, {name}님!')
```

### 3. 이미지 처리 라이브러리

#### Pillow (PIL)
- **용도**: 이미지 로딩, 저장, 변환
- **주요 기능**: 포맷 변환, 크기 조정, 필터 적용

#### OpenCV
- **용도**: 컴퓨터 비전, 이미지 분석
- **주요 기능**: 색상 변환, 객체 감지, 이미지 전처리

#### pyzbar
- **용도**: QR 코드 디코딩 (읽기)
- **원리**: 이미지 패턴 분석 → 데이터 추출

#### qrcode
- **용도**: QR 코드 생성 (인코딩)
- **원리**: 데이터 → 시각적 패턴 변환

---

## 🔨 실습 1: 개발 환경 설정

### Step 1: 의존성 파일 작성

프로젝트 폴더에 `requirements.txt` 파일을 생성하세요:

```text
# Streamlit 기반 QR 코드 리더기 & 생성기 의존성 패키지

# 웹 프레임워크
streamlit>=1.31.0

# 이미지 처리
Pillow>=10.0.0
opencv-python-headless>=4.8.0.74

# QR 코드 디코딩 (리더기)
pyzbar>=0.1.9

# QR 코드 생성 (생성기)
qrcode[pil]>=7.4.2
```

### Step 2: 패키지 설치

터미널에서 다음 명령어를 실행하세요:

```bash
pip install -r requirements.txt
```

**예상 출력**:
```
Collecting streamlit>=1.31.0
  Downloading streamlit-1.31.1-py3-none-any.whl (8.2 MB)
Collecting Pillow>=10.0.0
  ...
Successfully installed streamlit-1.31.1 Pillow-10.2.0 ...
```

### Step 3: 설치 확인

Python 인터프리터에서 확인:

```python
python
>>> import streamlit as st
>>> import qrcode
>>> import pyzbar
>>> print("모든 라이브러리 설치 완료!")
```

### ⚠️ Windows 사용자 주의사항

pyzbar를 사용하려면 **zbar 라이브러리** 추가 설치가 필요합니다:

```bash
# vcpkg 사용 (권장)
vcpkg install zbar:x64-windows
```

또는 [zbar 다운로드](http://zbar.sourceforge.net/download.html)에서 DLL 파일 다운로드 후 PATH에 추가

### ✅ 체크포인트 1

- [ ] requirements.txt 파일 생성 완료
- [ ] 모든 패키지 설치 완료
- [ ] import 테스트 성공

---

## 🔨 실습 2: QR 코드 리더기 구현

### Step 1: 기본 구조 만들기

`qr_reader.py` 파일을 생성하고 다음 코드를 작성하세요:

```python
"""
QR 코드 리더기 - Streamlit 기반
"""

import streamlit as st
from PIL import Image
import cv2
import numpy as np
from pyzbar.pyzbar import decode
from datetime import datetime
import re

# 페이지 설정
st.set_page_config(
    page_title="QR 코드 리더기",
    page_icon="📱",
    layout="centered"
)

# 타이틀
st.title("📱 QR 코드 리더기")
st.write("QR 코드가 포함된 이미지를 업로드하세요")
```

#### 💡 코드 설명

- `st.set_page_config()`: 페이지 제목, 아이콘, 레이아웃 설정
- `st.title()`: 큰 제목 표시
- `st.write()`: 일반 텍스트 표시

### Step 2: 파일 업로드 기능 추가

```python
# 파일 업로드
uploaded_file = st.file_uploader(
    "이미지 파일을 선택하세요",
    type=['png', 'jpg', 'jpeg']
)

if uploaded_file is not None:
    # 이미지 로드
    image = Image.open(uploaded_file)
    
    # 이미지 표시
    st.image(image, caption="업로드된 이미지", use_container_width=True)
```

#### 💡 코드 설명

- `st.file_uploader()`: 파일 업로드 위젯 생성
- `type`: 허용할 파일 확장자 지정
- `Image.open()`: PIL로 이미지 파일 열기
- `st.image()`: 이미지 표시

### Step 3: QR 코드 디코딩 함수 작성

파일 상단에 다음 함수를 추가하세요:

```python
def decode_qr_code(image):
    """
    이미지에서 QR 코드를 디코딩하는 함수
    
    Args:
        image (PIL.Image): Pillow 이미지 객체
        
    Returns:
        list: 디코딩된 QR 코드 데이터 리스트
    """
    # PIL 이미지를 NumPy 배열로 변환
    img_array = np.array(image)
    
    # RGB를 BGR로 변환 (OpenCV는 BGR 사용)
    if len(img_array.shape) == 3 and img_array.shape[2] == 3:
        img_bgr = cv2.cvtColor(img_array, cv2.COLOR_RGB2BGR)
    else:
        img_bgr = img_array
    
    # 그레이스케일로 변환 (QR 코드 인식률 향상)
    gray = cv2.cvtColor(img_bgr, cv2.COLOR_BGR2GRAY)
    
    # QR 코드 디코딩
    decoded_objects = decode(gray)
    
    return decoded_objects
```

#### 💡 코드 설명

1. **NumPy 변환**: PIL 이미지를 배열로 변환
2. **색상 변환**: RGB → BGR (OpenCV 형식)
3. **그레이스케일**: 색상 제거로 인식률 향상
4. **디코딩**: pyzbar로 QR 코드 패턴 분석

### Step 4: 결과 표시하기

```python
if uploaded_file is not None:
    # ... (이전 코드)
    
    # QR 코드 디코딩
    with st.spinner('QR 코드를 분석 중입니다...'):
        decoded_objects = decode_qr_code(image)
    
    # 결과 표시
    if decoded_objects:
        st.success(f"✅ {len(decoded_objects)}개의 QR 코드를 찾았습니다!")
        
        for idx, obj in enumerate(decoded_objects, 1):
            qr_data = obj.data.decode('utf-8')
            
            st.subheader(f"QR 코드 #{idx}")
            st.code(qr_data)
    else:
        st.error("❌ QR 코드를 찾을 수 없습니다.")
```

#### 💡 코드 설명

- `st.spinner()`: 로딩 중 표시
- `st.success()`: 성공 메시지 (녹색)
- `st.error()`: 에러 메시지 (빨간색)
- `obj.data.decode()`: 바이트를 문자열로 변환

### Step 5: 애플리케이션 실행

터미널에서 다음 명령어로 실행:

```bash
streamlit run qr_reader.py
```

브라우저가 자동으로 열리면서 `http://localhost:8501`에 접속됩니다.

### ✅ 체크포인트 2

- [ ] 기본 Streamlit 앱 생성 완료
- [ ] 파일 업로드 기능 작동
- [ ] QR 코드 디코딩 함수 구현
- [ ] 결과 표시 성공
- [ ] 애플리케이션 실행 테스트 완료

### 🧪 테스트하기

1. 온라인 QR 생성기로 테스트 QR 코드 생성: [qr-code-generator.com](https://www.qr-code-generator.com/)
2. 생성된 QR 코드 이미지 다운로드
3. 앱에 업로드하여 인식 확인

---

## 🔨 실습 3: QR 코드 생성기 구현

### Step 1: QR 생성 함수 작성

```python
import qrcode
import io

def generate_qr(data, fill_color='black', back_color='white', 
                box_size=10, border=4, error_correction='M'):
    """
    QR 코드를 생성하는 함수
    
    Args:
        data (str): QR 코드에 인코딩할 데이터
        fill_color (str): 전경색
        back_color (str): 배경색
        box_size (int): 박스 크기
        border (int): 테두리 두께
        error_correction (str): 오류 복구 수준
        
    Returns:
        PIL.Image: 생성된 QR 코드 이미지
    """
    # 오류 복구 수준 매핑
    error_correction_map = {
        'L': qrcode.constants.ERROR_CORRECT_L,
        'M': qrcode.constants.ERROR_CORRECT_M,
        'Q': qrcode.constants.ERROR_CORRECT_Q,
        'H': qrcode.constants.ERROR_CORRECT_H
    }
    
    # QR 코드 생성기 초기화
    qr = qrcode.QRCode(
        version=1,
        error_correction=error_correction_map.get(error_correction),
        box_size=box_size,
        border=border,
    )
    
    # 데이터 추가
    qr.add_data(data)
    qr.make(fit=True)
    
    # 이미지 생성
    qr_img = qr.make_image(fill_color=fill_color, back_color=back_color)
    
    # PIL Image로 변환
    img = qr_img.convert('RGB')
    
    return img
```

#### 💡 코드 설명

1. **오류 복구 매핑**: 문자열을 qrcode 상수로 변환
2. **QRCode 객체 생성**: 설정값으로 초기화
3. **데이터 추가**: `add_data()`로 인코딩할 내용 설정
4. **이미지 생성**: `make_image()`로 시각적 패턴 생성
5. **RGB 변환**: 바이트 스트림 저장을 위해 표준 PIL Image로 변환

### Step 2: 탭 UI로 기능 분리

기존 코드를 수정하여 탭을 추가하세요:

```python
st.title("📱 QR 코드 리더기 & 생성기")

# 탭 생성
tab1, tab2 = st.tabs(["📤 QR 읽기", "✨ QR 생성"])

# 탭 1: QR 코드 읽기
with tab1:
    st.markdown("### 이미지에서 QR 코드 읽기")
    # ... (기존 리더기 코드)

# 탭 2: QR 코드 생성
with tab2:
    st.markdown("### QR 코드 생성하기")
    # (다음 단계에서 작성)
```

### Step 3: 생성기 UI 구현

```python
with tab2:
    st.markdown("### QR 코드 생성하기")
    
    # 데이터 입력
    qr_data = st.text_input(
        "QR 코드로 변환할 텍스트 또는 URL",
        placeholder="https://example.com"
    )
    
    # 사이드바에 커스터마이징 옵션
    with st.sidebar:
        st.markdown("## 🎨 디자인 설정")
        
        fill_color = st.color_picker("전경색", "#000000")
        back_color = st.color_picker("배경색", "#FFFFFF")
        
        box_size = st.slider("박스 크기", 5, 20, 10)
        border = st.slider("테두리 두께", 1, 10, 4)
        
        error_correction = st.selectbox(
            "오류 복구 수준",
            ['L', 'M', 'Q', 'H'],
            index=1
        )
```

#### 💡 코드 설명

- `st.tabs()`: 탭 인터페이스 생성
- `with tab1:` / `with tab2:`: 각 탭의 내용 정의
- `st.sidebar`: 사이드바에 위젯 배치
- `st.color_picker()`: 색상 선택 위젯
- `st.slider()`: 슬라이더 위젯
- `st.selectbox()`: 드롭다운 선택

### Step 4: QR 코드 생성 및 표시

```python
    # QR 코드 생성
    if qr_data:
        try:
            # 생성
            qr_image = generate_qr(
                data=qr_data,
                fill_color=fill_color,
                back_color=back_color,
                box_size=box_size,
                border=border,
                error_correction=error_correction
            )
            
            st.success("✅ QR 코드가 생성되었습니다!")
            
            # 미리보기
            st.image(qr_image, caption="생성된 QR 코드")
            
            # 다운로드 버튼
            img_bytes = io.BytesIO()
            qr_image.save(img_bytes, format='PNG')
            img_bytes.seek(0)
            
            st.download_button(
                label="📥 다운로드",
                data=img_bytes,
                file_name="my_qrcode.png",
                mime="image/png"
            )
            
        except Exception as e:
            st.error(f"오류 발생: {str(e)}")
    else:
        st.info("👆 텍스트를 입력하면 QR 코드가 생성됩니다")
```

#### 💡 코드 설명

1. **조건부 실행**: 입력값이 있을 때만 생성
2. **예외 처리**: try-except로 오류 포착
3. **바이트 스트림**: `io.BytesIO()`로 메모리 내 파일 생성
4. **다운로드 버튼**: `st.download_button()`으로 파일 제공

### ✅ 체크포인트 3

- [ ] QR 생성 함수 구현 완료
- [ ] 탭 UI 구현 완료
- [ ] 사이드바 커스터마이징 옵션 추가
- [ ] QR 코드 생성 및 표시 성공
- [ ] 다운로드 기능 작동

---

## 🎨 심화 학습

### 1. Custom CSS 적용

더 세련된 UI를 위해 CSS를 추가하세요:

```python
st.markdown("""
    <style>
    .main {
        background-color: #FFFFFF;
    }
    
    .stButton>button {
        background-color: #1e40af;
        color: white;
        border-radius: 8px;
        padding: 0.5rem 2rem;
        font-weight: 600;
    }
    
    .stButton>button:hover {
        background-color: #1e3a8a;
    }
    </style>
""", unsafe_allow_html=True)
```

### 2. 세션 상태로 히스토리 관리

```python
# 세션 상태 초기화
if 'scan_history' not in st.session_state:
    st.session_state.scan_history = []

# 히스토리에 추가
def add_to_history(data):
    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    st.session_state.scan_history.insert(0, {
        'time': timestamp,
        'data': data
    })
    # 최대 10개만 유지
    if len(st.session_state.scan_history) > 10:
        st.session_state.scan_history = st.session_state.scan_history[:10]

# 히스토리 표시
if st.session_state.scan_history:
    st.markdown("### 📜 스캔 히스토리")
    for item in st.session_state.scan_history:
        st.text(f"{item['time']} - {item['data'][:50]}")
```

### 3. URL 자동 감지

```python
import re

def is_url(text):
    """URL 형식 검증"""
    url_pattern = re.compile(r'^(https?://|www\.)[^\s]+$')
    return bool(url_pattern.match(text))

# 사용 예
if is_url(qr_data):
    st.link_button("🌐 사이트 방문", qr_data)
else:
    st.code(qr_data)
```

### 4. 카메라 입력 추가

```python
camera_image = st.camera_input("카메라로 QR 코드 촬영")

if camera_image is not None:
    image = Image.open(camera_image)
    decoded_objects = decode_qr_code(image)
    # ... 결과 처리
```

---

## 📝 과제

### 🎯 과제 1: 기본 기능 완성 (필수)

다음 기능을 모두 구현하세요:

1. ✅ QR 코드 리더기 (파일 업로드)
2. ✅ QR 코드 생성기 (텍스트 입력)
3. ✅ 커스터마이징 옵션 (색상, 크기)
4. ✅ 다운로드 기능

**제출**: 완성된 `qr_reader.py` 파일

### 🎯 과제 2: UI/UX 개선 (권장)

다음 중 2가지 이상 구현:

1. Custom CSS로 테마 디자인
2. 히스토리 기능 (최근 스캔 기록)
3. URL 자동 감지 및 링크 버튼
4. 여러 QR 코드 동시 인식
5. 카메라 촬영 기능

**제출**: 개선된 코드 + 스크린샷

### 🎯 과제 3: 창의적 확장 (도전)

자유 주제로 기능 추가:

**예시 아이디어**:
- 📊 QR 코드 사용 통계 (생성/스캔 횟수)
- 🎨 QR 코드에 로고 삽입
- 📱 SMS, 전화번호, WiFi 설정 QR 생성
- 💾 데이터베이스에 QR 기록 저장
- 🌐 다국어 지원
- 📤 클라우드 저장 (Google Drive, Dropbox)

**제출**: 확장 기능 설명 + 코드 + 데모 영상

---

## 🤔 자주 묻는 질문 (FAQ)

### Q1: pyzbar 설치 시 오류가 발생해요

**A**: Windows 사용자는 zbar 라이브러리 추가 설치가 필요합니다.

```bash
vcpkg install zbar:x64-windows
```

또는 [zbar.sourceforge.net](http://zbar.sourceforge.net/download.html)에서 DLL 다운로드

### Q2: QR 코드 인식이 안 돼요

**A**: 다음을 확인하세요:
- ✅ 이미지가 선명한가요?
- ✅ QR 코드 전체가 프레임에 들어왔나요?
- ✅ 조명이 충분한가요?
- ✅ 파일 형식이 지원되나요? (PNG, JPG, JPEG)

### Q3: 색상을 바꿨는데 QR 코드가 안 읽혀요

**A**: QR 리더기는 명암 대비로 인식합니다. 전경색과 배경색의 대비가 충분해야 합니다.

**좋은 예**: 검정(#000000) + 흰색(#FFFFFF)  
**나쁜 예**: 노란색(#FFFF00) + 흰색(#FFFFFF) - 대비 부족

### Q4: Streamlit 앱이 느려요

**A**: 다음을 시도하세요:
- `@st.cache_data` 데코레이터로 함수 캐싱
- 이미지 크기를 적절히 조정
- 불필요한 재실행 방지

```python
@st.cache_data
def decode_qr_code(image):
    # ... 함수 내용
```

### Q5: 앱을 배포하고 싶어요

**A**: Streamlit Community Cloud를 사용하면 무료로 배포 가능합니다:

1. GitHub에 코드 업로드
2. [share.streamlit.io](https://share.streamlit.io) 접속
3. 저장소 연결 및 배포

---

## 📚 참고 자료

### 공식 문서
- [Streamlit 공식 문서](https://docs.streamlit.io/)
- [qrcode 라이브러리 문서](https://pypi.org/project/qrcode/)
- [pyzbar 문서](https://github.com/NaturalHistoryMuseum/pyzbar)
- [Pillow 문서](https://pillow.readthedocs.io/)
- [OpenCV Python 튜토리얼](https://docs.opencv.org/4.x/d6/d00/tutorial_py_root.html)

### 추가 학습 자료
- [QR 코드 작동 원리 (영상)](https://www.youtube.com/watch?v=w5ebcowAJD8)
- [Streamlit 튜토리얼 시리즈](https://www.youtube.com/playlist?list=PLgkF0qak9G49QlteBWQ-h-e1lFcQg5K_I)
- [Python 이미지 처리 기초](https://realpython.com/image-processing-with-the-python-pillow-library/)

### 유용한 도구
- [온라인 QR 코드 생성기](https://www.qr-code-generator.com/)
- [QR 코드 테스터](https://qrcodescan.in/)
- [컬러 팔레트 생성](https://coolors.co/)
- [HTML/CSS 색상 코드](https://htmlcolorcodes.com/)

---

## 💬 수업 후 활동

### 📸 프로젝트 공유
완성된 프로젝트를 다음에 공유해보세요:
- GitHub에 저장소 생성
- README.md 작성
- Streamlit Community Cloud에 배포

### 🤝 피어 리뷰
동료의 코드를 리뷰하며 다음을 확인하세요:
- ✅ 코드 가독성 (주석, 함수명)
- ✅ 예외 처리
- ✅ UI/UX 직관성
- ✅ 추가 기능의 창의성

### 📖 다음 단계
이 프로젝트를 완료했다면, 다음 주제를 도전해보세요:
- 🧠 이미지 분류 AI 웹앱 (TensorFlow + Streamlit)
- 📊 데이터 시각화 대시보드 (Plotly + Streamlit)
- 💬 챗봇 웹앱 (OpenAI API + Streamlit)
- 📸 얼굴 인식 시스템 (OpenCV + Streamlit)

---

## ✅ 최종 체크리스트

수업 완료 확인:

### 이론 이해
- [ ] QR 코드의 구조와 원리 이해
- [ ] Streamlit의 작동 방식 이해
- [ ] 이미지 처리 라이브러리 역할 이해
- [ ] 오류 복구 수준 개념 이해

### 실습 완료
- [ ] 개발 환경 설정 완료
- [ ] QR 리더기 구현 완료
- [ ] QR 생성기 구현 완료
- [ ] 커스터마이징 기능 구현
- [ ] 애플리케이션 정상 실행 확인

### 코딩 실력
- [ ] 함수 설계 및 모듈화
- [ ] 예외 처리 구현
- [ ] 사용자 인터랙션 처리
- [ ] 세션 상태 관리
- [ ] 파일 입출력 처리

---

<div align="center">

## 🎉 수고하셨습니다!

이제 여러분은 **실용적인 웹 애플리케이션**을 만들 수 있습니다!

배운 내용을 활용하여 **자신만의 창의적인 프로젝트**를 만들어보세요! 🚀

**질문이나 도움이 필요하면 언제든 문의하세요!**

---

**만든 이**: QR 코드 튜토리얼 팀  
**최종 수정**: 2026-02-05  
**라이선스**: MIT License

</div>
